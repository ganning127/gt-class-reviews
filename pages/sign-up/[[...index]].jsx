import { SignUp } from "@clerk/nextjs";

const SignUpPage = () => (
    <center>
        <title>Sign Up | GT Class Reviews</title>

        <SignUp
        // path="/sign-up"
        // routing="path"
        // signInUrl="/sign-in"
        // redirectUrl="/"
        />
    </center>
);

export default SignUpPage;